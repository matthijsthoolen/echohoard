generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// EH-01 intentionally has no domain models. Feature tasks add them through
// subsequent migrations; this schema proves the production migration path.
